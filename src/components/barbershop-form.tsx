type Props = { action: (formData: FormData) => void | Promise<void>; defaults?: { name: string; slug: string; phone: string | null; email: string | null; address: string | null; timezone: string; currency: string }; submitLabel: string };
export function BarbershopForm({ action, defaults, submitLabel }: Props) { return <form action={action} className="mt-6 grid gap-4"><label>Nombre<input name="name" required defaultValue={defaults?.name} className="input" /></label><label>Slug<input name="slug" required defaultValue={defaults?.slug} pattern="[a-z0-9]+(-[a-z0-9]+)*" className="input" /></label><label>Teléfono<input name="phone" defaultValue={defaults?.phone ?? ""} className="input" /></label><label>Email<input name="email" type="email" defaultValue={defaults?.email ?? ""} className="input" /></label><label>Dirección<input name="address" defaultValue={defaults?.address ?? ""} className="input" /></label><label>Zona horaria<select name="timezone" defaultValue={defaults?.timezone ?? "America/Santiago"} className="input"><option value="America/Santiago">America/Santiago</option></select></label><label>Moneda<select name="currency" defaultValue={defaults?.currency ?? "CLP"} className="input"><option value="CLP">CLP — Peso chileno</option></select></label><button className="rounded-md bg-slate-900 px-4 py-2 font-medium text-white">{submitLabel}</button></form>; }
