import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
type Props = { action: (formData: FormData) => void | Promise<void>; defaults?: { name: string; slug: string; phone: string | null; email: string | null; address: string | null; timezone: string; currency: string }; submitLabel: string };
export function BarbershopForm({ action, defaults, submitLabel }: Props) { return <form action={action} className="grid gap-5"><div className="grid gap-2"><Label>Nombre</Label><Input name="name" required defaultValue={defaults?.name ?? ""} /></div><div className="grid gap-2"><Label>Slug</Label><Input name="slug" required defaultValue={defaults?.slug ?? ""} pattern="[a-z0-9]+(-[a-z0-9]+)*" /></div><div className="grid gap-4 sm:grid-cols-2"><div className="grid gap-2"><Label>Teléfono</Label><Input name="phone" defaultValue={defaults?.phone ?? ""} /></div><div className="grid gap-2"><Label>Email</Label><Input name="email" type="email" defaultValue={defaults?.email ?? ""} /></div></div><div className="grid gap-2"><Label>Dirección</Label><Input name="address" defaultValue={defaults?.address ?? ""} /></div><div className="grid gap-4 sm:grid-cols-2"><div className="grid gap-2"><Label>Zona horaria</Label><Input name="timezone" readOnly defaultValue={defaults?.timezone ?? "America/Santiago"} /></div><div className="grid gap-2"><Label>Moneda</Label><Input name="currency" readOnly defaultValue={defaults?.currency ?? "CLP"} /></div></div><div className="flex justify-end"><Button type="submit">{submitLabel}</Button></div></form>; }
