import Link from "next/link";
import { register } from "@/app/actions/auth";
export default async function RegisterPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="grid min-h-screen place-items-center bg-slate-100 p-6"><section className="w-full max-w-md rounded-xl bg-white p-7 shadow-sm"><h1 className="text-2xl font-bold">Crea tu cuenta</h1>{error && <p className="mt-4 rounded-md bg-red-50 p-3 text-sm text-red-700">{error}</p>}<form action={register} className="mt-6 space-y-4"><label className="block text-sm font-medium">Nombre<input name="name" required className="input mt-1" /></label><label className="block text-sm font-medium">Correo<input name="email" type="email" required className="input mt-1" /></label><label className="block text-sm font-medium">Contraseña<input name="password" type="password" minLength={8} required className="input mt-1" /></label><button className="w-full rounded-md bg-slate-900 px-4 py-2 font-medium text-white">Crear cuenta</button></form><p className="mt-5 text-sm text-slate-600">¿Ya tienes cuenta? <Link className="font-medium text-slate-900 underline" href="/login">Ingresa</Link></p></section></main>; }
